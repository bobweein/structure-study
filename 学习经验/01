用变量做函数的参数：
  实参向形参的数据传递是“值传递”，单向传递，只能由实参传给形参，而不能有形参传给实参。实参和形参在内存中占有不同的储存单元，实参无法得到形参的值。
用数组名作函数参数：
  不是吧数组元素的值传递给形参，二十八实参数组的首元素的地址传递给形参数组，这样两个数组就共占同一段内存单元，也就是说形参数组中的个元素发生变化会是实参数组的值同时发生改变。这一点是与变量做函数参数的情况不同的。
